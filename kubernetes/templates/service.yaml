---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Release.Name }}
    app.kubernetes.io/component: svc
    app.kubernetes.io/name: keycloak-broker
  name: {{ .Release.Name }}
spec:
  ports:
  - port: 8080
    name: web
    protocol: TCP
    targetPort: 8080
  selector:
    app: {{ .Release.Name }}
    app.kubernetes.io/component: app
    app.kubernetes.io/name: keycloak-broker
  type: ClusterIP
